[package]
name = "sctt-physics"
version = "0.1.0"
edition = "2021"

[dependencies]
sctt-core = { path = "../sctt-core" }
nalgebra = "0.32"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]